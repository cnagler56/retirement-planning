# Copy to backend/.env (gitignored) and set a password. docker-compose reads this
# automatically for the MySQL root password.
#
# Note: MySQL only applies the root password the FIRST time the data volume is
# created. If you already started the DB with a different password, either reuse
# that same value here or recreate the volume:  docker compose down -v
MYSQL_ROOT_PASSWORD=change_me

package com.home;

import org.junit.jupiter.api.Test;

/**
 * Placeholder unit test. A full {@code @SpringBootTest} context load is skipped
 * here because it needs a live MySQL datasource; add slice tests (@WebMvcTest,
 * @DataJpaTest) as features land.
 */
class RetireServerApplicationTests {

	@Test
	void contextConfigurationIsPresent() {
		// Intentionally trivial — keeps `mvn test` green before infra services exist.
	}

}
